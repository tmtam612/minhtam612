@model PagedList.IPagedList<QLXS.Models.KetQuaXoSo>
@using PagedList.Mvc;
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh Sách</h2>
@using (Html.BeginForm())
{
    <p>
        <label>Tìm Kiếm:</label> @Html.TextBox("SearchString")
        <input type="submit" value="Search" />
    </p>
}
<p>
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().MaKetQua)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.First().LoaiVeSo.Tinh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Ngay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().SoTrung)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaKetQua)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoaiVeSo.Tinh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ngay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoTrung)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.MaKetQua }) |
                @Html.ActionLink("Details", "Details", new { id = item.MaKetQua }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.MaKetQua })
            </td>
        </tr>
    }

</table>
<br />
Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index",
    new { page }))
